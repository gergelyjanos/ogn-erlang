{
    application, 
    ogn_erlang, 
    [
        {description, "OGN collector in Erlang"},
        {vsn, "1.0.0"},
        {modules, [
            ogn_app,
            ogn_sup,
            http_server_sup,
            http_server_server,
            web
            % aircraftpositionapi,
            % aircraftPositionDb,
            % aprsclient,
            % deployhelper,
            % httpserver,
            % json,
            % parser,
            % receiverpositionapi,
            % receiverPositionDb,
            % startup,
            % systeminfoapi,
            % systeminfoDb
            ]
        },
        {registered, [
            ogn_sup,
            http_server_sup,
            http_server_server,
            web
            % httpserver,
            % aircraftPositionDb,
            % receiverPositionDb,
            % systeminfodb
            ]
        },
        {applications, [kernel, stdlib ]},
        {mod, {ogn_app, []}}
    ]
}
.
